// vite.preload.config.mts (The Security Bridge): This bundles the highly secure middleman script. It sits between the frontend and backend, passing messages back and forth so the visual screen never directly touches the database.

import { defineConfig } from "vite";

// https://vitejs.dev/config
export default defineConfig({});
